{
  "name": "socketapp",
  "version": "1.0.0",
  "description": "This is basic backend for socket app using http module",
  "main": "index.js",
  "scripts": {
    "start": "nodemon node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "suhail",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
